<section class="hero">
	<div class="hero__content">
		<p class="hero__eyebrow">RE-CONECTA CON TU</p>
		<h1 class="hero__title">SANADOR INTERIOR</h1>
		<div class="hero__divider"></div>
		<p class="hero__details">Viernes 20- Domingo 22 de Febrero</p>
		<p class="hero__details">Hunkapi - Valle de Bravo</p>
	</div>
</section>

<style>
	.hero {
		position: relative;
		min-height: 100vh;
		display: grid;
		place-items: center;
		text-align: center;
		color: #fff;
	}

	.hero__content {
		position: relative;
		padding: 6rem 1.5rem;
		max-width: 720px;
		z-index: 1;
	}

	.hero__eyebrow {
		font-size: clamp(1.1rem, 2vw, 1.5rem);
		letter-spacing: 0.35rem;
		margin: 0 0 0.6rem;
		text-transform: uppercase;
	}

	.hero__title {
		font-size: clamp(2.4rem, 6vw, 4.5rem);
		margin: 0 0 1.5rem;
		letter-spacing: 0.25rem;
		text-transform: uppercase;
	}

	.hero__divider {
		width: 140px;
		height: 2px;
		margin: 0 auto 1.5rem;
		background: rgba(255, 255, 255, 0.7);
	}

	.hero__details {
		margin: 0.35rem 0;
		font-size: clamp(1rem, 2vw, 1.25rem);
	}
</style>
